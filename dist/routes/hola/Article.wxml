<block>
    <view class="article">
        <view class="box" wx:key="{{i}}" wx:for="{{loopArray0}}" wx:for-item="item"
        wx:for-index="i">
            <image src="{{item.cover}}" mode="widthFix" />
            <view class="content">
                <view class="title">{{item.title}}</view>
                <view class="desc">{{item.desc}}</view>
                <view class="type">{{item.tag}}
                    <view>{{item.$loopState__temp2}}</view>
                </view>
            </view>
        </view>
    </view>
</block>