<block>
    <view class="{{rootClassName}}">
        <view class="at-activity-indicator__body">
            <at-loading size="{{size}}" __triggerObserer="{{ _triggerObserer }}" color="{{color}}"></at-loading>
        </view>
        <block wx:if="{{content}}">
            <text class="at-activity-indicator__content">{{content}}</text>
        </block>
    </view>
</block>